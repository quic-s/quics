{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "runStartCommand",
            "type": "shell",
            "command": "go",
            "args": ["build", "-o", "qis", "${workspaceFolder}/cmd"],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "runListenCommand",
            "type": "shell",
            "command": "go",
            "args": ["build", "-o", "qis", "${workspaceFolder}/cmd"],
            "group": {
                "kind": "build",
                "isDefault": false
            }
        }
    ]
}